services:
  - type: web
    name: license-api
    env: python
    plan: free
    pythonVersion: "3.10"  # ระบุเวอร์ชัน Python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000